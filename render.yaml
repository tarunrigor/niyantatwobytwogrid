services:
  - type: web
    name: twc-positioning-grid
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    envVars:
      - key: NODE_VERSION
        value: 18.20.4
      - key: CI
        value: false
